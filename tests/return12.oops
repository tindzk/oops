CLASS Main IS
    METHOD main IS BEGIN END METHOD

    METHOD fib2(s1, s2: Integer; x: Integer): Integer IS BEGIN
        IF TRUE THEN
            RETURN s1; 
        ELSE
            | This branch is irrelevant as the first one always evalutes to `true'.
            IF s2 = 0 THEN
                RETURN 0;
            ELSEIF s2 = 1 THEN
                RETURN 1;
            END IF
        END IF
    END METHOD
END CLASS